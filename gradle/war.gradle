apply plugin: "war"

bootWar {
    mainClassName = "com.icthh.xm.ms.dashboard.GateApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
