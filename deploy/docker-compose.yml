version: '3.3'
services:
    dashboard-app:
        image: xmonline/xm-ms-dashboard:${IMAGE_TAG:-latest}
        networks:
            - xm2
        env_file:
            - ./env/dashboard-app.env
        secrets:
            - source: SPRING_DATASOURCE_PASSWORD
              target: SPRING_DATASOURCE_PASSWORD
            - source: SPRING_DATASOURCE_URL
              target: SPRING_DATASOURCE_URL
            - source: SPRING_DATASOURCE_USERNAME
              target: SPRING_DATASOURCE_USERNAME
        deploy:
            mode: replicated
            replicas: 1
            restart_policy:
                condition: on-failure
        logging:
            driver: syslog
            options:
                tag: dashboard
                syslog-facility: local7
secrets:
    SPRING_DATASOURCE_PASSWORD:
        external: true
    SPRING_DATASOURCE_URL:
        external: true
    SPRING_DATASOURCE_USERNAME:
        external: true
networks:
    xm2:
        driver: overlay
